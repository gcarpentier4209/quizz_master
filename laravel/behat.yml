default:
  extensions:
    Laracasts\Behat:
    # env_path: .env.behat
    Behat\MinkExtension:
      default_session: laravel
      laravel: ~

    Behat\MinkExtension\Extension:
      goutte: ~
      selenium2: ~